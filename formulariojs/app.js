const d = document,
  $body = d.querySelector("body");
d.addEventListener("submit", (e) => {
  if (e.target.matches("#")) {
    const $form = d.getElementById("");

    if ($form.user === "admin" && $form.password === "admin") {
    }
  }
});
